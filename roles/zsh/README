## Run the following command to install Zsh with Oh My Zsh and plugins (Requires SUDO)

```shell
ansible-playbook -vvv main.yml -i "localhost" --connection="localhost" --extra-vars "state=present" -t="zsh" -K
```