## Run the following command to install virt-manager (Requires SUDO)

```bash
ansible-playbook -vvv main.yml -i "localhost" --connection="localhost" --extra-vars "state=present" -t="virtmanager" -K
```